   <?php
   // Примеры переменных
   $variable = 3.14;
   // $variable = 1;
   // $variable = 'one';
   // $variable = true;
   // $variable = null;
   // $variable = [];

   // Определение типа переменной
   if (is_bool($variable)) {
       $type = 'bool';
   } elseif (is_float($variable)) {
       $
   }